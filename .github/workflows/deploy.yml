name: Build and Publish Backend Image

on:
  push:
    branches: [main]
    paths-ignore:
      - "client/**"
      - "**.md"
  workflow_dispatch:

env:
  IMAGE: ghcr.io/${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}

  # Optional: deploy to a k8s cluster automatically.
  # Requires a repo secret KUBE_CONFIG containing your cluster's kubeconfig
  # (base64-encoded). Uncomment once you have a live cluster.
  #
  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: azure/setup-kubectl@v4
  #     - name: Configure kubeconfig
  #       run: |
  #         mkdir -p ~/.kube
  #         echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > ~/.kube/config
  #     - name: Deploy
  #       run: |
  #         kubectl apply -f k8s/
  #         kubectl set image deployment/ecommerce-backend backend=${{ env.IMAGE }}:${{ github.sha }}
  #         kubectl rollout status deployment/ecommerce-backend --timeout=180s
